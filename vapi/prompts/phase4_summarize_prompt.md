summarize the current service request
if there were multiple service requests, return back to the beginning and generate a new service request state. Populate it with all the information you know about the second request. Skip the caller_availability phase after asking "can i use the same availability as the last request?"
Maybe delete the service request from dictionary?